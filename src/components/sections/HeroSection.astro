---
import HeroKollegScroller from './HeroKollegScroller';

const { id } = Astro.props;
---

<section id={id} class="relative w-full min-h-screen grid grid-rows-[1fr_auto] bg-transparent">
  <HeroKollegScroller client:visible className="lg:mt-20 mt-6" />
</section>
