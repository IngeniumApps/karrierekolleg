---
import TypewriterText from '../visual/animation/TypeWriterText';
import UnderlineBrush from '../visual/animation/UnderlineBrush';
const { id } = Astro.props;
---

<section
    id={id}
    class="relative w-full min-h-screen pt-16 pb-24 sm:pb-32 flex items-center justify-center overflow-hidden px-6"
>
  <!-- 🔮 Bewegter Gradient-Hintergrund (sanft animiert für Chrome + Safari) -->
  <div
      class="absolute inset-0 z-0"
      style="
      background: radial-gradient(ellipse at top right, #1b95cc44, transparent 90%);
      will-change: transform;
    "
  ></div>

  <!-- 🧊 Glass-Overlay (Chrome-kompatibel) -->
  <div class="absolute inset-0 z-0 backdrop-glass"></div>

  <!-- 💬 Inhalt -->
  <div class="relative z-10 text-center w-full max-w-6xl px-4 sm:px-6 py-12 sm:py-16">
    <h1 class="text-[8vw] sm:text-4xl md:text-5xl lg:text-6xl font-headline font-bold mb-8 leading-tight text-gray-900">
      <span class="inline-block font-black text-[10vw] sm:text-6xl md:text-7xl lg:text-8xl text-primary">
        <TypewriterText
            client:idle
            words={['Karriere', 'Zukunft', 'Bildung']}
            loop={Infinity}
            cursorStyle="_"
        />
      </span>
      <br />
      neu denken.<br />
      <span class="relative inline-block text-[11vw] sm:text-6xl md:text-7xl lg:text-8xl">
        <span class="relative z-[1] text-primary">Kolleg</span>
        <UnderlineBrush
            className="hidden lg:block absolute left-0 bottom-0 z-0 w-full"
            fillColor="#f4c74b"
            bottomOffset={5}
            client:visible
        />
      </span>
      starten.
    </h1>

    <p class="text-base sm:text-lg md:text-xl text-gray-700 max-w-2xl mx-auto mb-10 leading-relaxed">
      Mit einem Kolleg eröffnest du dir neue Wege – praxisnah, berufsbegleitend und staatlich anerkannt.
    </p>

    <a
        href={`${import.meta.env.BASE_URL}#vorteile`}
        class="animate-wiggle animate-infinite animate-ease-linear inline-block bg-white text-primary font-bold border border-primary px-8 py-4 rounded-full shadow-md hover:bg-primary hover:text-white transition-transform transform hover:scale-105 motion-safe:animate-bounce-in"
    >
      Jetzt informieren
    </a>
  </div>

  <!-- 🧭 Scroll-Indikator -->
  <div class="absolute bottom-6 left-1/2 -translate-x-1/2 text-gray-500 text-sm animate-bounce z-10">
    ↓ Scroll weiter
  </div>
</section>