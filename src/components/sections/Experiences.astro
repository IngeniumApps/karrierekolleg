---
import { Testimonials } from '@components/testimonials/Testimonials';
import FadeDownOnScroll from '../visual/animation/FadeDownOnScroll';
import UnderlineBrush from '../visual/animation/UnderlineBrush';

const { id } = Astro.props;
---

<section
  id={id}
  class="relative flex flex-col w-full min-h-[100dvh] overflow-x-hidden"
  style="min-height: 100dvh"
>
  <FadeDownOnScroll client:visible duration={0.8}>
    <div class="text-center px-5 pt-20 lg:pt-32">
      <h2
        class="text-[8vw] sm:text-4xl md:text-5xl lg:text-6xl font-headline font-bold mb-8 leading-tight"
      >
        <span
          class="font-black relative inline-block text-[11vw] sm:text-6xl md:text-7xl lg:text-8xl"
        >
          <span class="relative z-[1] text-primary">Was andere </span>
          <UnderlineBrush
            className="hidden lg:block absolute left-0 bottom-0 z-0 w-full"
            fillColor="#BBF451"
            bottomOffset={8}
          />
        </span>
        <span class="text-[8vw] sm:text-4xl md:text-5xl lg:text-6xl"> über uns sagen.</span>
      </h2>
    </div>
    <p class="landscape:mb-6 text-center text-base sm:text-lg md:text-xl max-w-5xl mx-auto leading-relaxed">
      Stimmen, Eindrücke und Erfahrungen – gesammelt aus vielen Perspektiven rund ums Karriere
      Kolleg.
    </p>
  </FadeDownOnScroll>

  <Testimonials client:visible />
</section>
