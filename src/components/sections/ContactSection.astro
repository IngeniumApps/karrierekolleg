---
import ContactForm from '../forms/ContactForm';
const { id } = Astro.props;
---

<section
  id={id}
  class="relative w-full flex items-center justify-center bg-white px-5 py-24 sm:py-32"
  style="min-height: 100dvh;"
>
  <!-- Gradient-Background -->
  <div
    class="absolute inset-0 z-0"
    style="background: radial-gradient(ellipse at top right, #1b95cc44, transparent 90%); will-change: transform;"
  >
  </div>

  <!-- Glass-Overlay -->
  <div class="absolute inset-0 z-0 backdrop-glass"></div>

  <!-- Content -->
  <div class="relative z-10 max-w-5xl w-full text-center">
    <h2 class="text-4xl sm:text-5xl font-headline font-bold text-primary mb-4">Kontaktiere uns</h2>
    <p class="text-gray-600 text-lg mb-12 max-w-xl mx-auto">
      Du interessierst dich für ein Kolleg oder hast Fragen? Schreib uns – wir freuen uns auf deine
      Nachricht!
    </p>
    <ContactForm client:visible />
  </div>
</section>
