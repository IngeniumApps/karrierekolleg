---
import RecognitionVideoHLS from "./RecognitionVideo";
const { id } = Astro.props;
---

<section id={id} class="relative w-full min-h-screen grid grid-rows-[1fr_auto] bg-transparent">
  <!-- 🔮 Radialer Verlauf -->
  <div
      class="absolute inset-0 z-0"
      style="background: radial-gradient(ellipse at top right, #1b95cc44, transparent 90%); will-change: transform;"
  ></div>

  <!-- 🧊 Glass-Effekt -->
  <div class="absolute inset-0 z-0 backdrop-glass"></div>

  <!-- 🧩 Inhalt -->
  <div class="z-10 max-w-6xl mx-auto px-6 py-32 w-full text-center flex flex-col items-center justify-center gap-10">
    <div>
      <h2 class="text-3xl lg:text-4xl font-bold mb-4">Anerkennung & Bachelor</h2>
      <p class="text-lg max-w-2xl mx-auto">
        NQR6, Anrechenbarkeit und akademische Anerkennung.
      </p>
    </div>

    <RecognitionVideoHLS client:visible />
  </div>
</section>